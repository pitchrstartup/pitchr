# Primary database connection (required)
DATABASE_URL="postgresql://user:password@host:5432/pitchr?sslmode=require"

# Provided by Neon/Vercel - used for direct (non-pooled) connections (optional)
# DATABASE_URL_UNPOOLED="postgresql://user:password@host:5432/pitchr?sslmode=require"

# Session secret for cookie signing (optional for demo, use a random string in prod)
SESSION_SECRET="change-me-in-production"
